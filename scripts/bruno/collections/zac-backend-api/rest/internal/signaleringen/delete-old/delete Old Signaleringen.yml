info:
  name: delete Old Signaleringen
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseUrl}}/rest/internal/signaleringen/delete-old"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/internal/signaleringen/delete-old"
      method: DELETE
      headers:
        - name: Accept
          value: application/json
    response:
      statusText: "200"
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "deletedSignaleringenCount": 1122
          }
