info:
  name: read Files As Zip
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/rest/informatieobjecten/download/zip"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      [
        "string",
        "string"
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/informatieobjecten/download/zip"
      method: POST
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            "string",
            "string"
          ]
    response:
      statusText: "200"
      body:
        type: text
        data: ""
