info:
  name: read Overige Rechten
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/rest/policy/overigeRechten"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/policy/overigeRechten"
      method: GET
      headers:
        - name: Accept
          value: application/json
    response:
      statusText: "200"
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "startenZaak": false,
            "beheren": false,
            "zoeken": true
          }
