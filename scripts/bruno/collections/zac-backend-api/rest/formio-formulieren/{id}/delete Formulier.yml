info:
  name: delete Formulier
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseUrl}}/rest/formio-formulieren/:id"
  params:
    - name: id
      value: "651"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/formio-formulieren/:id"
      method: DELETE
      params:
        - name: id
          value: "651"
          type: path
    response:
      statusText: "200"
      body:
        type: text
        data: ""
