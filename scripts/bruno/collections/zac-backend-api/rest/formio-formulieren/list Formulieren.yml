info:
  name: list Formulieren
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/rest/formio-formulieren"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/formio-formulieren"
      method: GET
      headers:
        - name: Accept
          value: application/json
    response:
      statusText: "200"
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "name": "string",
              "title": "string",
              "id": 8361
            },
            {
              "name": "string",
              "title": "string",
              "id": 813
            }
          ]
