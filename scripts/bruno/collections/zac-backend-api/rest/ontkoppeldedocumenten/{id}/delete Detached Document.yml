info:
  name: delete Detached Document
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseUrl}}/rest/ontkoppeldedocumenten/:id"
  params:
    - name: id
      value: "651"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: No Content
    request:
      url: "{{baseUrl}}/rest/ontkoppeldedocumenten/:id"
      method: DELETE
      params:
        - name: id
          value: "651"
          type: path
    response:
      statusText: "204"
      body:
        type: text
        data: ""
