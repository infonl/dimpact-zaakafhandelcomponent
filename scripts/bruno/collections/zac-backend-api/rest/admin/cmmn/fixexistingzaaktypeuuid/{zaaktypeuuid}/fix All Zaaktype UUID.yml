info:
  name: fix All Zaaktype UUID
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/rest/admin/cmmn/fixexistingzaaktypeuuid/:zaaktypeuuid"
  params:
    - name: zaaktypeuuid
      value: string
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/admin/cmmn/fixexistingzaaktypeuuid/:zaaktypeuuid"
      method: GET
      params:
        - name: zaaktypeuuid
          value: string
          type: path
    response:
      statusText: "200"
      body:
        type: text
        data: ""
