info:
  name: log Existing Zaaktype UUID
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/rest/admin/cmmn/logzaaktypeuuid"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/admin/cmmn/logzaaktypeuuid"
      method: GET
    response:
      statusText: "200"
      body:
        type: text
        data: ""
