info:
  name: read Gemeente Code
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/rest/configuratie/gemeente/code"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/configuratie/gemeente/code"
      method: GET
      headers:
        - name: Accept
          value: application/json
    response:
      statusText: "200"
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: string
