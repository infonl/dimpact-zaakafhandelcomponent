info:
  name: list Task Form Definitions
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/rest/zaakafhandelparameters/formulierdefinities"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/zaakafhandelparameters/formulierdefinities"
      method: GET
      headers:
        - name: Accept
          value: application/json
    response:
      statusText: "200"
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "id": "string",
              "veldDefinities": [
                {
                  "naam": "string",
                  "waarde": "string"
                },
                {
                  "naam": "string",
                  "waarde": "string"
                }
              ]
            },
            {
              "id": "string",
              "veldDefinities": [
                {
                  "naam": "string",
                  "waarde": "string"
                },
                {
                  "naam": "string",
                  "waarde": "string"
                }
              ]
            }
          ]
