info:
  name: remove
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseUrl}}/rest/:job-id"
  params:
    - name: job-id
      value: string
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: No Content
    request:
      url: "{{baseUrl}}/rest/:job-id"
      method: DELETE
      params:
        - name: job-id
          value: string
          type: path
    response:
      statusText: "204"
      body:
        type: text
        data: ""
