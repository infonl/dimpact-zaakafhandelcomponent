info:
  name: read And Remove
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/rest/:job-id?wait=-1"
  params:
    - name: wait
      value: "-1"
      type: query
    - name: job-id
      value: string
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/:job-id?wait=-1"
      method: POST
      params:
        - name: wait
          value: "-1"
          type: query
        - name: job-id
          value: string
          type: path
    response:
      statusText: "200"
      body:
        type: text
        data: ""
