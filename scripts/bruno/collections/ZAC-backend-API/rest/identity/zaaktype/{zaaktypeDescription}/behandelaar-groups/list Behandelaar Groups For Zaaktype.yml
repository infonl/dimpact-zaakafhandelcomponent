info:
  name: list Behandelaar Groups For Zaaktype
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/rest/identity/zaaktype/:zaaktypeDescription/behandelaar-groups"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: zaaktypeDescription
      value: string
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/identity/zaaktype/:zaaktypeDescription/behandelaar-groups"
      method: GET
      headers:
        - name: Accept
          value: application/json
      params:
        - name: zaaktypeDescription
          value: string
          type: path
    response:
      statusText: "200"
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "id": "string",
              "naam": "string"
            },
            {
              "id": "string",
              "naam": "string"
            }
          ]
