info:
  name: count Missing Variables
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/rest/admin/cmmn/countmissingvariables"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/admin/cmmn/countmissingvariables"
      method: GET
    response:
      statusText: "200"
      body:
        type: text
        data: ""
