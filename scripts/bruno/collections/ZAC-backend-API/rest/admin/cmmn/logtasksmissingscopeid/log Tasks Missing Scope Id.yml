info:
  name: log Tasks Missing Scope Id
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/rest/admin/cmmn/logtasksmissingscopeid"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/admin/cmmn/logtasksmissingscopeid"
      method: GET
    response:
      statusText: "200"
      body:
        type: text
        data: ""
