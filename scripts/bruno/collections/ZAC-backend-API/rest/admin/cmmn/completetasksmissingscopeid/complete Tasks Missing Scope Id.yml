info:
  name: complete Tasks Missing Scope Id
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/rest/admin/cmmn/completetasksmissingscopeid"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/admin/cmmn/completetasksmissingscopeid"
      method: GET
    response:
      statusText: "200"
      body:
        type: text
        data: ""
