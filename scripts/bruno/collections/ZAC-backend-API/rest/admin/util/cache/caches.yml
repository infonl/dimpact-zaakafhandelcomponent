info:
  name: caches
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/rest/admin/util/cache"
  headers:
    - name: Accept
      value: text/html
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/admin/util/cache"
      method: GET
      headers:
        - name: Accept
          value: text/html
    response:
      statusText: "200"
      headers:
        - name: Content-Type
          value: text/html
      body:
        type: html
        data: string
