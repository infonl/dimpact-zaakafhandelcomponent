info:
  name: ztc Caches
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/rest/admin/util/cache/ztc"
  headers:
    - name: Accept
      value: text/html
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/admin/util/cache/ztc"
      method: GET
      headers:
        - name: Accept
          value: text/html
    response:
      statusText: "200"
      headers:
        - name: Content-Type
          value: text/html
      body:
        type: html
        data: string
