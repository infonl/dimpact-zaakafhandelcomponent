info:
  name: clear All Sensitive Data Caches
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/rest/admin/util/sensitive-data/clear"
  headers:
    - name: Accept
      value: text/html
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/admin/util/sensitive-data/clear"
      method: GET
      headers:
        - name: Accept
          value: text/html
    response:
      statusText: "200"
      headers:
        - name: Content-Type
          value: text/html
      body:
        type: html
        data: string
