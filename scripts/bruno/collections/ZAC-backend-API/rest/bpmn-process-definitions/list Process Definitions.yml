info:
  name: list Process Definitions
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/rest/bpmn-process-definitions"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/bpmn-process-definitions"
      method: GET
      headers:
        - name: Accept
          value: application/json
    response:
      statusText: "200"
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "id": "string",
              "name": "string",
              "key": "string",
              "version": 9931
            },
            {
              "id": "string",
              "name": "string",
              "key": "string",
              "version": 2497
            }
          ]
