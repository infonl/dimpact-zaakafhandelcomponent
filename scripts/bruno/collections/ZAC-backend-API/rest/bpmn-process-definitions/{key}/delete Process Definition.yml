info:
  name: delete Process Definition
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseUrl}}/rest/bpmn-process-definitions/:key"
  params:
    - name: key
      value: string
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/bpmn-process-definitions/:key"
      method: DELETE
      params:
        - name: key
          value: string
          type: path
    response:
      statusText: "200"
      body:
        type: text
        data: ""
