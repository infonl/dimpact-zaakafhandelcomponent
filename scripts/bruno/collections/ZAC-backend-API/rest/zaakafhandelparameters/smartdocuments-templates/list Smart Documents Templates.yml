info:
  name: list Smart Documents Templates
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/rest/zaakafhandelparameters/smartdocuments-templates"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/zaakafhandelparameters/smartdocuments-templates"
      method: GET
      headers:
        - name: Accept
          value: application/json
    response:
      statusText: "200"
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "id": "string",
              "name": "string",
              "groups": null,
              "templates": null
            },
            {
              "id": "string",
              "name": "string",
              "groups": null,
              "templates": [
                {
                  "id": "string",
                  "name": "string"
                }
              ]
            }
          ]
