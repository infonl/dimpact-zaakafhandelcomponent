info:
  name: list Communication Channels
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/rest/referentietabellen/communicatiekanaal/:inclusiefEFormulier"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: inclusiefEFormulier
      value: "true"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: OK
    request:
      url: "{{baseUrl}}/rest/referentietabellen/communicatiekanaal/:inclusiefEFormulier"
      method: GET
      headers:
        - name: Accept
          value: application/json
      params:
        - name: inclusiefEFormulier
          value: "true"
          type: path
    response:
      statusText: "200"
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            "string",
            "string"
          ]
