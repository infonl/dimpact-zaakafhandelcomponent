# SmartDocuments WireMock mappings

The mappings folder contains SmartDocuments [WireMock](https://wiremock.org/) mappings.
These mappings are used in our integration tests.
Therefore, be careful when changing them.
