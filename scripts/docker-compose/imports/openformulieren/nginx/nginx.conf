server {
    listen 8080;

    client_max_body_size 50m;

    location / {
        proxy_pass http://openformulieren-web:8000;
        proxy_set_header Host $http_host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
