{
  "auth-server-url": "${env.AUTH_SERVER}/",
  "realm": "${env.AUTH_REALM}",
  "client-id": "${env.AUTH_RESOURCE}",
  "credentials": {
    "secret": "${env.AUTH_SECRET}"
  },
  "ssl-required": "${env.AUTH_SSL_REQUIRED}",
  "principal-attribute": "preferred_username"
}
