#
# SPDX-FileCopyrightText: 2023 INFO.nl
# SPDX-License-Identifier: EUPL-1.2+
#
name: Cleanup cache after PR close

on:
  pull_request:
    types:
      - closed

permissions:
  contents: write
  actions: write

jobs:
  cleanup-branch-cache:
    runs-on: ubuntu-26.04
    steps:
      - uses: Friedinger/DeleteBranchCaches@90efbd35252dc8ea465daf9952cc8a523fbf3e2b # v2.4.5
        with:
          github-token: ${{ github.token }}
          # Specify explicitly because the ref at the time of merging will be a branch name such as 'main', 'develop'
          ref: refs/pull/${{ github.event.pull_request.number }}/merge
