#
# SPDX-FileCopyrightText: 2024 INFO.nl
# SPDX-License-Identifier: EUPL-1.2+
#
services:
  keycloak-database:
    image: postgres:17.10@sha256:5c855ad7b85e68e48a62f34662853f38b57c1c1d80f3a927ab58034fd6d31c5e
    ports:
      - "54326:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}" ]
      interval: 5s
      timeout: 10s
      retries: 10
    environment:
      - POSTGRES_USER=keycloak
      - POSTGRES_PASSWORD=keycloak
      - POSTGRES_DB=keycloak
    volumes:
      - ./scripts/docker-compose/volume-data/zac-keycloak-database-data:/var/lib/postgresql/data

  keycloak:
    image: quay.io/keycloak/keycloak:26.6.4@sha256:0aae0de7fca85525f727d3354df17896092de8bb26ae4c12d89c77e5df8cbce4
    depends_on:
      keycloak-database:
        condition: service_healthy
    ports:
      - "127.0.0.1:8081:8080"
      - "9001:9000"
    healthcheck:
      test: [
        "CMD-SHELL",
        "exec 3<>/dev/tcp/127.0.0.1/9000; echo -e 'GET /health/ready HTTP/1.1\r\nHost: localhost:9000\r\nConnection: close\r\n\r\n' >&3;cat <&3 | grep -q '\"status\": \"UP\"' && exit 0 || exit 1"
      ]
      interval: 5s
      timeout: 10s
      retries: 10
    environment:
      - KC_BOOTSTRAP_ADMIN_USERNAME=admin
      - KC_BOOTSTRAP_ADMIN_PASSWORD=admin
      - KC_HEALTH_ENABLED=true
      # This requires adding host.docker.internal to your `/etc/hosts` file.
      # See the "Steps to Add host.docker.internal Entry to /etc/hosts File" section in `installDockerCompose.md`.
      - KC_HOSTNAME=http://host.docker.internal:8081
      - KC_HTTP_ENABLED=true
      - KC_HOSTNAME_BACKCHANNEL_DYNAMIC=true
      - KC_DB=postgres
      - KC_DB_URL_HOST=keycloak-database
      - KC_DB_URL_DATABASE=keycloak
      - KC_DB_USERNAME=keycloak
      - KC_DB_PASSWORD=keycloak
      - KC_IMPORT=/opt/keycloak/data/import/zaakafhandelcomponent-realm.json
      - ZAC_RAADPLEGER_TEST_1_EMAIL_ADDRESS=${DOCKER_COMPOSE_RAADPLEGER_TEST_1_EMAIL_ADDRESS:-raadpleger-test-1@example.com}
      - ZAC_RAADPLEGER_TEST_2_EMAIL_ADDRESS=${DOCKER_COMPOSE_RAADPLEGER_TEST_2_EMAIL_ADDRESS:-raadpleger-test-2@example.com}
      - ZAC_BEHANDELAAR_TEST_1_EMAIL_ADDRESS=${DOCKER_COMPOSE_BEHANDELAAR_TEST_1_EMAIL_ADDRESS:-behandelaar-test-1@example.com}
      - ZAC_BEHANDELAAR_TEST_2_EMAIL_ADDRESS=${DOCKER_COMPOSE_BEHANDELAAR_TEST_2_EMAIL_ADDRESS:-behandelaar-test-2@example.com}
      - ZAC_COORDINATOR_TEST_1_EMAIL_ADDRESS=${DOCKER_COMPOSE_COORDINATOR_TEST_1_EMAIL_ADDRESS:-coordinator-test-1@example.com}
      - ZAC_COORDINATOR_TEST_2_EMAIL_ADDRESS=${DOCKER_COMPOSE_COORDINATOR_TEST_2_EMAIL_ADDRESS:-coordinator-test-2@example.com}
      - ZAC_RECORDMANAGER_TEST_1_EMAIL_ADDRESS=${DOCKER_COMPOSE_RECORDMANAGER_TEST_1_EMAIL_ADDRESS:-recordmanager-test-1@example.com}
      - ZAC_RECORDMANAGER_TEST_2_EMAIL_ADDRESS=${DOCKER_COMPOSE_RECORDMANAGER_TEST_2_EMAIL_ADDRESS:-recordmanager-test-2@example.com}
      - ZAC_BEHEERDER_TEST_1_EMAIL_ADDRESS=${DOCKER_COMPOSE_BEHEERDER_TEST_1_EMAIL_ADDRESS:-beheerder-test-1@example.com}
      - ZAC_RAADPLEGER_EN_BEHANDELAAR_TEST_1_EMAIL_ADDRESS=${DOCKER_COMPOSE_RAADPLEGER_EN_BEHANDELAAR_TEST_1_EMAIL_ADDRESS:-raadpleger-en-behandelaar-test-1@example.com}
      - ZAC_GROUP_RAADPLEGERS_TEST_1_EMAIL_ADDRESS=${DOCKER_COMPOSE_GROUP_RAADPLEGERS_TEST_1_EMAIL_ADDRESS:-test-group-raadplegers-test-1@example.com}
      - ZAC_GROUP_RAADPLEGERS_TEST_2_EMAIL_ADDRESS=${DOCKER_COMPOSE_GROUP_RAADPLEGERS_TEST_2_EMAIL_ADDRESS:-test-group-raadplegers-test-2@example.com}
      - ZAC_GROUP_BEHANDELAARS_TEST_1_EMAIL_ADDRESS=${DOCKER_COMPOSE_GROUP_BEHANDELAARS_TEST_1_EMAIL_ADDRESS:-test-group-behandelaars-test-1@example.com}
      - ZAC_GROUP_BEHANDELAARS_TEST_2_EMAIL_ADDRESS=${DOCKER_COMPOSE_GROUP_BEHANDELAARS_TEST_2_EMAIL_ADDRESS:-test-group-behandelaars-test-2@example.com}
      - ZAC_GROUP_COORDINATORS_TEST_1_EMAIL_ADDRESS=${DOCKER_COMPOSE_GROUP_COORDINATORS_TEST_1_EMAIL_ADDRESS:-test-group-coordinators-test-1@example.com}
      - ZAC_GROUP_COORDINATORS_TEST_2_EMAIL_ADDRESS=${DOCKER_COMPOSE_GROUP_COORDINATORS_TEST_2_EMAIL_ADDRESS:-test-group-coordinators-test-2@example.com}
      - ZAC_GROUP_RECORDMANAGERS_TEST_1_EMAIL_ADDRESS=${DOCKER_COMPOSE_GROUP_RECORDMANAGERS_TEST_1_EMAIL_ADDRESS:-test-group-recordmanagers-test-1@example.com}
      - ZAC_GROUP_RECORDMANAGERS_TEST_2_EMAIL_ADDRESS=${DOCKER_COMPOSE_GROUP_RECORDMANAGERS_TEST_2_EMAIL_ADDRESS:-test-group-recordmanagers-test-2@example.com}
      - ZAC_GROUP_BEHEERDERS_ELK_DOMEIN_EMAIL_ADDRESS=${DOCKER_COMPOSE_GROUP_BEHEERDERS_ELK_DOMEIN_EMAIL_ADDRESS:-test-group-beheerders-elk-domein@example.com}
      - ZAC_GROUP_BEHANDELAARS_LONG_NAME_TEST_EMAIL_ADDRESS=${DOCKER_COMPOSE_GROUP_BEHANDELAARS_LONG_NAME_TEST_EMAIL_ADDRESS:-test-group-behandelaars-long-name-test@example.com}
    volumes:
      - ./scripts/docker-compose/imports/keycloak/realms:/opt/keycloak/data/import
    command:
      - "start-dev"
      - "--import-realm"

  openzaak-database:
    image: postgis/postgis:17-3.4@sha256:d0b5a6ecab18997637f55a83cb4a9467391de5645916cfa1b6f2a8d19eee7be5
    platform: linux/amd64
    ports:
      - "54322:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
      interval: 5s
      timeout: 10s
      retries: 30
    environment:
      - POSTGRES_USER=openzaak
      - POSTGRES_PASSWORD=openzaak
      - POSTGRES_DB=openzaak
      - BAG_API_CLIENT_MP_REST_URL=${BAG_API_CLIENT_MP_REST_URL:-https://api.bag.kadaster.nl/lvbag/individuelebevragingen/v2/}
      - BAG_API_KEY=${BAG_API_KEY:-please-request-a-bag-api-key}
    volumes:
      - ./scripts/docker-compose/imports/openzaak-database:/docker-entrypoint-initdb.d
      - ./scripts/docker-compose/volume-data/openzaak-database-data:/var/lib/postgresql/data

  # This container name must contain a '.' or else Open Zaak will respond with a 400
  # error on certain API requests caused by an internal 'invalid URL' error.
  openzaak.local:
    image: openzaak/open-zaak:1.29.1@sha256:e872cbfd6df6521736f521797f96726dcdb622c83340c621c7fce9c530f6bcdf
    platform: linux/amd64
    environment: &openzaak-env
      - ALLOWED_HOSTS="*"
      - DB_HOST=openzaak-database
      - DB_NAME=openzaak
      - DB_PASSWORD=openzaak
      - DB_USER=openzaak
      - CACHE_DEFAULT=redis:6379/0
      - CACHE_AXES=redis:6379/0
      - DEBUG=true
      - DISABLE_2FA=true
      - DJANGO_SETTINGS_MODULE=openzaak.conf.docker
      - DJANGO_SUPERUSER_PASSWORD=admin
      - LOG_LEVEL=DEBUG
      # uncomment to enable logging of outgoing requests; can be helpful for debugging
      #- LOG_REQUESTS=true
      - NOTIFICATIONS_DISABLED=${OPENZAAK_NOTIFICATIONS_DISABLED:-true}
      - OPENZAAK_SUPERUSER_USERNAME=admin
      - OPENZAAK_SUPERUSER_EMAIL=noreply@example.com
      - SECRET_KEY=openZaakSecretKey
      - SENDFILE_BACKEND=django_sendfile.backends.simple
      - CELERY_BROKER_URL=redis://redis:6379/1
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
      - CELERY_LOGLEVEL=DEBUG
      - OTEL_SDK_DISABLED=true
    volumes:
      - ./scripts/docker-compose/imports/openzaak/zac-scripts:/zac-scripts
      - ./scripts/docker-compose/imports/openzaak/uploads/fake-test-document.pdf:/fake-test-document.pdf
    command: ["/zac-scripts/copy-test-pdf-and-start-openzaak.sh"]
    healthcheck:
      test: [ "CMD", "python", "-c", "import requests; exit(requests.head('http://localhost:8000/admin/').status_code not in [200, 302])" ]
      interval: 5s
      timeout: 10s
      retries: 30
      # This should allow for enough time for migrations to run before the max
      # retries have passed. This health check in turn allows other containers
      # to wait for the database migrations.
      start_period: 10s
    depends_on:
      openzaak-database:
        condition: service_healthy
      redis:
        condition: service_started

  # Open Zaak Celery worker
  # Used for sending notifications from Open Zaak to Open Notificaties
  openzaak-celery:
    image: openzaak/open-zaak:1.29.1@sha256:e872cbfd6df6521736f521797f96726dcdb622c83340c621c7fce9c530f6bcdf
    platform: linux/amd64
    environment: *openzaak-env
    command: /celery_worker.sh
    healthcheck:
      test: [ "CMD", "python", "/app/bin/check_celery_worker_liveness.py" ]
      interval: 5s
      timeout: 10s
      retries: 10
      start_period: 10s
    depends_on:
      openzaak.local:
        condition: service_healthy
    profiles: [ "opennotificaties", "openformulieren" ]

  # nginx reverse proxy in front of Open Zaak.
  # Needed to handle ZAC's chunked transfer encoding (Transfer-Encoding: chunked) for large
  # file uploads. uwsgi in HTTP mode does not buffer chunked request bodies before passing
  # them to Django. nginx buffers the full request before forwarding it with Content-Length,
  # matching the production Kubernetes setup where nginx also sits in front of Open Zaak.
  openzaak-nginx:
    image: nginxinc/nginx-unprivileged:1.31.3@sha256:13e6db27178b9bbd0964daa10cfd1027b7d6b17ee718591c9a0e4cb35cbe9d84
    ports:
      - "8001:8000"
    volumes:
      - ./scripts/docker-compose/imports/openzaak/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      openzaak.local:
        condition: service_healthy

  redis:
    image: redis:8.6.4@sha256:4483474d5e78c444ce180037def4430ec0d02553663ded2a6d7a1c922da00ecf

  # This container name must contain a '.' or else Open Zaak will respond with a 400
  # error on certain API requests caused by an internal 'invalid URL' error.
  objecten-api.local:
    image: maykinmedia/objects-api:3.6.2@sha256:6a3a40081016e5072c5355622c0ca3e1ded89228edce7336fc4d8600217344f8
    platform: linux/amd64
    ports:
      - "8010:8000"
    environment: &objects-env
      - ALLOWED_HOSTS="*"
      - CORS_ALLOW_ALL_ORIGINS=true
      - DB_HOST=objecten-api-database
      - DEBUG=true
      - DISABLE_2FA=true
      - DJANGO_SETTINGS_MODULE=objects.conf.docker
      - SECRET_KEY=objectenApiSecretKey
      - TWO_FACTOR_FORCE_OTP_ADMIN=False
      - TWO_FACTOR_PATCH_ADMIN=False
      - CACHE_DEFAULT=redis:6379/0
      - CACHE_AXES=redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/1
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
      - EMAIL_HOST=localhost
      - OTEL_SDK_DISABLED=true
    healthcheck:
      test: "bash -c 'printf \"GET / HTTP/1.1\n\n\" > /dev/tcp/127.0.0.1/8000; exit $?;'"
      interval: 5s
      timeout: 10s
      retries: 10
    depends_on:
      objecten-api-database:
        condition: service_healthy
    profiles: [ "objecten", "itest", "openformulieren" ]

  objecten-api-celery:
    image: maykinmedia/objects-api:3.6.2@sha256:6a3a40081016e5072c5355622c0ca3e1ded89228edce7336fc4d8600217344f8
    platform: linux/amd64
    environment: *objects-env
    command: sh -c "cd /app/src && celery -A objects worker -l info"
    healthcheck:
      test: [ "CMD", "python", "/app/bin/check_celery_worker_liveness.py" ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    depends_on:
      objecten-api.local:
        condition: service_healthy
      redis:
        condition: service_started
    profiles: [ "objecten", "itest", "openformulieren" ]

  objecten-api-database:
    image: postgis/postgis:17-3.4@sha256:d0b5a6ecab18997637f55a83cb4a9467391de5645916cfa1b6f2a8d19eee7be5
    platform: linux/amd64
    ports:
      - "54323:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
      interval: 5s
      timeout: 10s
      retries: 20
    environment:
      - POSTGRES_USER=objects
      - POSTGRES_PASSWORD=objects
      - POSTGRES_DB=objects
    profiles: [ "objecten", "itest", "openformulieren" ]

  objecten-api-import:
    image: maykinmedia/objects-api:3.6.2@sha256:6a3a40081016e5072c5355622c0ca3e1ded89228edce7336fc4d8600217344f8
    platform: linux/amd64
    environment: *objects-env
    # in the current version of django it is not possible to create a new user with password without user interaction by using the createsuperuser command
    command: sh init/init.sh
    volumes:
      - ./scripts/docker-compose/imports/objects-api/fixtures:/app/src/objects/fixtures
      - ./scripts/docker-compose/imports/objects-api/init:/app/init
    depends_on:
      objecten-api.local:
        condition: service_healthy
    profiles: [ "objecten", "itest", "openformulieren" ]

  solr:
    image: solr:9.10.1-slim@sha256:c45d9d93d74d6a5e996120ab7120cdfc531debbc59bd3d6a6736fe240cee9be0
    ports:
      - "8983:8983"
    volumes:
      - ./scripts/docker-compose/volume-data/solr-data:/var/solr
    # run as root to change the permissions of the solr folder
    user: root
    # Change permissions of the solr folder, create the 'zac' core and start solr
    command: bash -c "
      chown -R 8983:8983 /var/solr
      && runuser -u solr -- solr-precreate zac"

  # Open Policy Agent (OPA)
  opa:
    # Keep this OPA binary version in sync with the one used in the ZAC Helm chart (values.yaml file)
    image: openpolicyagent/opa:1.18.2-static@sha256:57f7d06808fff6de3ea1d698e6430990973ca1370be0e54975f0083d615521da
    command: run --server --addr=0.0.0.0:8181 --log-level debug
    ports:
      - "8181:8181"

  brp-personen-wiremock:
    image: wiremock/wiremock:3.13.2@sha256:0d4ecb3e4dc8213fd7a4d37d6a78f6e6b553a6d2e15bd51b0999781282ac61b3
    volumes:
      - ./scripts/docker-compose/imports/brp-personen-wiremock/mappings:/home/wiremock/mappings
    entrypoint: [ "/docker-entrypoint.sh", "--disable-gzip", "--verbose" ]
    ports:
      - "18084:8080"
    depends_on:
      brp-personen-mock:
        condition: service_started

  brp-personen-mock:
    image: ghcr.io/brp-api/personen-mock:2.7.0-202606230850@sha256:024308250beab8a6327d8f4ce91e630b4bcbd650015a4e86df6db437dc8bb4d7
    environment:
      - ASPNETCORE_ENVIRONMENT=Release
      - ASPNETCORE_URLS=http://+:5010
    ports:
      - "5010:5010"

  openklant-database:
    image: postgres:17.10@sha256:5c855ad7b85e68e48a62f34662853f38b57c1c1d80f3a927ab58034fd6d31c5e
    ports:
      - "54325:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
      interval: 5s
      timeout: 10s
      retries: 20
    environment:
      - POSTGRES_USER=openklant
      - POSTGRES_PASSWORD=openklant
      - POSTGRES_DB=openklant
    volumes:
      - ./scripts/docker-compose/imports/openklant-database:/docker-entrypoint-initdb.d
      - ./scripts/docker-compose/volume-data/openklant-database-data:/var/lib/postgresql/data

  openklant.local:
    image: maykinmedia/open-klant:2.15.0@sha256:ce59a2c60dab1c14e62fe34a99511839390dd2d482983c27334241e0d60c693d
    platform: linux/amd64
    environment:
      - ALLOWED_HOSTS="*"
      - DB_HOST=openklant-database
      - DB_USER=openklant
      - DB_PASSWORD=openklant
      - DB_NAME=openklant
      - CACHE_DEFAULT=redis:6379/0
      - CACHE_AXES=redis:6379/0
      - DJANGO_SETTINGS_MODULE=openklant.conf.docker
      - EMAIL_HOST=localhost
      - IS_HTTPS=no
      - NOTIFICATIONS_DISABLED=True
      - SECRET_KEY=openZaakSecretKey
      - DISABLE_2FA=true
    ports:
      - "8002:8000"
    depends_on:
      openklant-database:
        condition: service_healthy
      redis:
        condition: service_started

  smartdocuments-wiremock:
    image: wiremock/wiremock:3.13.2@sha256:0d4ecb3e4dc8213fd7a4d37d6a78f6e6b553a6d2e15bd51b0999781282ac61b3
    volumes:
      # these WireMock mappings are used by the ZAC integration tests
      - ./scripts/docker-compose/imports/smartdocuments-wiremock/mappings:/home/wiremock/mappings
      - ./scripts/docker-compose/imports/smartdocuments-wiremock/__files:/home/wiremock/__files
    entrypoint: [ "/docker-entrypoint.sh", "--global-response-templating", "--disable-gzip", "--verbose" ]
    ports:
      - "18080:8080"
    profiles: [ "itest" ]

  kvk-wiremock:
    image: wiremock/wiremock:3.13.2@sha256:0d4ecb3e4dc8213fd7a4d37d6a78f6e6b553a6d2e15bd51b0999781282ac61b3
    volumes:
      # these WireMock mappings are used by the ZAC integration tests
      - ./scripts/docker-compose/imports/kvk-wiremock/mappings:/home/wiremock/mappings
      - ./scripts/docker-compose/imports/kvk-wiremock/__files:/home/wiremock/__files
    entrypoint: [ "/docker-entrypoint.sh", "--global-response-templating", "--disable-gzip", "--verbose" ]
    ports:
      - "18081:8080"
    profiles: [ "itest" ]

  # This container name must contain a '.' or else Open Zaak will respond with a 400
  # error on certain API requests caused by an internal 'invalid URL' error.
  bag-wiremock.local:
    image: wiremock/wiremock:3.13.2@sha256:0d4ecb3e4dc8213fd7a4d37d6a78f6e6b553a6d2e15bd51b0999781282ac61b3
    volumes:
      # these WireMock mappings are used by the ZAC integration tests
      - ./scripts/docker-compose/imports/bag-wiremock/mappings:/home/wiremock/mappings
      - ./scripts/docker-compose/imports/bag-wiremock/__files:/home/wiremock/__files
    entrypoint: [ "/docker-entrypoint.sh", "--global-response-templating", "--disable-gzip", "--verbose" ]
    ports:
      - "18082:8080"
    profiles: [ "itest" ]

  opa-tests:
    # Keep this OPA binary version in sync with the one used in the ZAC Helm chart (values.yaml file)
    image: openpolicyagent/opa:1.18.2@sha256:cba27d3c6af2feba1e4d6e6b5e24df5b53db332420d4148a90acccd12efae6ed
    volumes:
      - ./src/test/resources/policies:/home/tests
      - ./src/main/resources/policies:/home/policies
    entrypoint: [ "opa", "test", "/home/policies", "/home/tests" ]
    profiles: [ "itest" ]

  office-converter:
    # When updating this Docker image please also update the Office Converter version accordingly in our Helm Chart (see values.yaml).
    image: gotenberg/gotenberg:8.34.0@sha256:67097317623a503ba2a6a7e9ae8db6929a1f7e1bbd88077bacf2d325fbdab923
    # Disable Chromium routes since we only use LibreOffice for document conversion.
    command:
      - "gotenberg"
      - "--chromium-disable-routes=true"
    ports:
      - "8083:3000"

  # Open Telemetry Collector which can receive OTLP observability data sent by ZAC
  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.156.0@sha256:125bdbeb7590cc1952c5b3430ecf14063568980c2c93d5b38676cc0446ed8108
    command: [ --config=/etc/otel-collector.yaml ]
    volumes:
      - ./scripts/docker-compose/imports/otel-collector/otel-collector.yaml:/etc/otel-collector.yaml:Z
    ports:
      - "1888:1888" # pprof extension
      - "13133:13133" # health_check extension
      - "4317:4317" # OTLP gRPC receiver
      - "4318:4318" # OTLP http receiver
      - "55679:55679" # zpages extension
    depends_on:
      - tempo
    profiles: [ "metrics" ]

  # Observability traces data store
  tempo:
    image: grafana/tempo:3.0.2@sha256:cda87c212d8c584dc0b89e337e7ed648a5100feb657e5d528480ee4fa03dbbe3
    command: [ "-config.file=/etc/tempo.yaml" ]
    volumes:
      - ./scripts/docker-compose/imports/tempo/tempo.yaml:/etc/tempo.yaml
    ports:
      - "14268"  # jaeger ingest
      - "3200:3200"   # tempo
      - "4317"  # otlp grpc
      - "4318"  # otlp http
      - "9411"   # zipkin
    profiles: [ "metrics" ]

  # Observability metrics data store
  prometheus:
    image: prom/prometheus:v3.13.1@sha256:3c42b892cf723fa54d2f262c37a0e1f80aa8c8ddb1da7b9b0df9455a35a7f893
    command:
      - --config.file=/etc/prometheus.yaml
      - --web.enable-remote-write-receiver
      - --enable-feature=exemplar-storage
    volumes:
      - ./scripts/docker-compose/imports/prometheus/prometheus.yaml:/etc/prometheus.yaml
    ports:
      - "9090:9090"
    profiles: [ "metrics" ]

  # Visualizes metrics and traces data
  grafana:
    image: grafana/grafana:13.1.0@sha256:121a7a9ece6dc10b969f1f96eed64b4f07dfac0d0b8abc070f7cb83bbde86f63
    volumes:
      - ./scripts/docker-compose/imports/grafana/grafana-datasources.yaml:/etc/grafana/provisioning/datasources/datasources.yaml
      - ./scripts/docker-compose/volume-data/grafana-data:/var/lib/grafana
    environment:
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
      - GF_AUTH_DISABLE_LOGIN_FORM=true
      - GF_FEATURE_TOGGLES_ENABLE=traceqlEditor
    ports:
      - "3000:3000"
    healthcheck:
      test: "bash -c 'printf \"GET /api/health HTTP/1.1\n\n\" > /dev/tcp/127.0.0.1/3000; exit $?;'"
      interval: 5s
      timeout: 10s
      retries: 20
    depends_on:
      - tempo
      - prometheus
    profiles: [ "metrics" ]

  greenmail:
    image: greenmail/standalone:2.1.11@sha256:53cf220d34e4f2ca9e216f9ae5d9051624780ee510740370b8026915094b87fa
    environment:
      GREENMAIL_OPTS: >-
        -Dgreenmail.smtp.hostname=0.0.0.0
        -Dgreenmail.smtp.port=25
        -Dgreenmail.hostname=0.0.0.0
        -Dgreenmail.auth.disabled
        -Dgreenmail.verbose
    ports:
      - "25:25"
      - "143:143"
      - "18083:8080"
    profiles: [ "itest" ]

  rabbitmq:
    image: rabbitmq:4.2.7-alpine@sha256:5e6dba3baccbfcaf273ef24139d1b14de9ec5423d9683fc005c72c480aa6eae9
    healthcheck:
      test: rabbitmq-diagnostics -q ping
      interval: 10s
      timeout: 10s
      retries: 20
    profiles: [ "opennotificaties", "openformulieren" ]

  opennotificaties-database:
    image: postgis/postgis:17-3.4@sha256:d0b5a6ecab18997637f55a83cb4a9467391de5645916cfa1b6f2a8d19eee7be5
    platform: linux/amd64
    ports:
      - "54327:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}" ]
      interval: 5s
      timeout: 10s
      retries: 20
    environment:
      - POSTGRES_USER=opennotificaties
      - POSTGRES_PASSWORD=opennotificaties
      - POSTGRES_DB=opennotificaties
    command: postgres -c max_connections=300 -c log_min_messages=LOG
    volumes:
      - ./scripts/docker-compose/imports/opennotificaties-database:/docker-entrypoint-initdb.d
      - ./scripts/docker-compose/volume-data/opennotificaties-database-data:/var/lib/postgresql/data
    profiles: [ "opennotificaties", "openformulieren" ]

  opennotificaties:
    image: openzaak/open-notificaties:1.15.0@sha256:5a2f7e04efe6062b71ae9e1b4bef2bf16f2f60c8f1d8b9632fbeab4b90c91c5a
    platform: linux/amd64
    environment: &opennotificaties-env
      ALLOWED_HOSTS: "*"
      CACHE_DEFAULT: redis:6379/0
      CACHE_AXES: redis:6379/0
      CELERY_BROKER_URL: amqp://guest:guest@rabbitmq:5672//
      CELERY_RESULT_BACKEND: redis://redis:6379/1
      CELERY_LOGLEVEL: DEBUG
      DB_HOST: opennotificaties-database
      DB_NAME: opennotificaties
      DB_PASSWORD: opennotificaties
      DB_USER: opennotificaties
      DEBUG: true
      DISABLE_2FA: true
      DJANGO_SETTINGS_MODULE: nrc.conf.docker
      DJANGO_SUPERUSER_PASSWORD: admin
      LOG_LEVEL: DEBUG
      LOG_NOTIFICATIONS_IN_DB: true
      OPENNOTIFICATIES_SUPERUSER_USERNAME: admin
      OPENNOTIFICATIES_SUPERUSER_EMAIL: admin@example.com
      PUBLISH_BROKER_URL: amqp://guest:guest@rabbitmq:5672/%2F
      RABBITMQ_HOST: rabbitmq
      SECRET_KEY: opennotificatiesApiSecretKey
    healthcheck:
      test: [ "CMD", "python", "-c", "import requests; exit(requests.head('http://localhost:8000/admin/').status_code not in [200, 302])" ]
      interval: 5s
      timeout: 10s
      retries: 10
      # This should allow for enough time for migrations to run before the max
      # retries have passed.
      # This health check in turn allows other containers to wait for the database migrations.
      start_period: 10s
    ports:
      - "8003:8000"
    volumes: &opennotificaties-volumes
      - ./scripts/docker-compose/imports/opennotificaties/setup-configuration:/app/setup_configuration
    depends_on:
      opennotificaties-init:
        condition: service_completed_successfully
    profiles: [ "opennotificaties", "openformulieren" ]

  opennotificaties-init:
    image: openzaak/open-notificaties:1.15.0@sha256:5a2f7e04efe6062b71ae9e1b4bef2bf16f2f60c8f1d8b9632fbeab4b90c91c5a
    platform: linux/amd64
    environment:
      <<: *opennotificaties-env
      # Run Django setup configuration
      RUN_SETUP_CONFIG: ${RUN_SETUP_CONFIG:-true}
    command: /setup_configuration.sh
    depends_on:
      opennotificaties-database:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_started
    volumes: *opennotificaties-volumes
    profiles: [ "opennotificaties", "openformulieren" ]

  opennotificaties-celery:
    image: openzaak/open-notificaties:1.15.0@sha256:5a2f7e04efe6062b71ae9e1b4bef2bf16f2f60c8f1d8b9632fbeab4b90c91c5a
    platform: linux/amd64
    environment: *opennotificaties-env
    command: /celery_worker.sh
    healthcheck:
      test: [ "CMD", "python", "/app/bin/check_celery_worker_liveness.py" ]
      interval: 5s
      timeout: 10s
      retries: 10
      start_period: 10s
    depends_on:
      opennotificaties:
        condition: service_healthy
    profiles: [ "opennotificaties", "openformulieren" ]

  openarchiefbeheer-database:
    image: postgis/postgis:17-3.4@sha256:d0b5a6ecab18997637f55a83cb4a9467391de5645916cfa1b6f2a8d19eee7be5
    platform: linux/amd64
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}" ]
      interval: 5s
      timeout: 10s
      retries: 10
    environment:
      - POSTGRES_USER=openarchiefbeheer
      - POSTGRES_PASSWORD=openarchiefbeheer
      - POSTGRES_DB=openarchiefbeheer
    ports:
      - "54328:5432"
    volumes:
      - ./scripts/docker-compose/imports/openarchiefbeheer-database:/docker-entrypoint-initdb.d
      - ./scripts/docker-compose/volume-data/openarchiefbeheer-database-data:/var/lib/postgresql/data
    profiles: [ "openarchiefbeheer" ]

  openarchiefbeheer-web:
    image: maykinmedia/open-archiefbeheer:2.0.0@sha256:e5217d748869c62d26393311c3cfdcaacf69d8dde80ca3bc172ff41f3885f0ff
    platform: linux/amd64
    environment: &web_env
      - ALLOWED_HOSTS="*"
      - DJANGO_SETTINGS_MODULE=openarchiefbeheer.conf.docker
      - SECRET_KEY=${SECRET_KEY:-oabSecretKey}
      - DB_NAME=openarchiefbeheer
      - DB_USER=openarchiefbeheer
      - DB_PASSWORD=openarchiefbeheer
      - DB_HOST=openarchiefbeheer-database
      - CACHE_DEFAULT=redis:6379/0
      - CACHE_AXES=redis:6379/0
      - CORS_ALLOWED_ORIGINS=http://localhost:8005,http://localhost:8004
      - CSRF_TRUSTED_ORIGINS=http://localhost:8005,http://localhost:8004
      - CSRF_COOKIE_SAMESITE=Lax
      - CSRF_COOKIE_SECURE=False
      - SESSION_COOKIE_SAMESITE=Lax
      - SESSION_COOKIE_SECURE=False
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
      - CELERY_LOGLEVEL=DEBUG
      - OAB_API_URL=http://localhost:8004
      - OAB_API_PATH=/api/v1
      - OAB_ZAAK_URL_TEMPLATE=https://localhost:8080/zaken/{identificatie}
      - FRONTEND_URL=http://localhost:8005
      - REQUESTS_READ_TIMEOUT=5000
    # Only this service serves the admin login form, so only here do we need to
    # override DJANGO_SETTINGS_MODULE to disable enforced two-factor authentication.
    command: [ "sh", "-c", "DJANGO_SETTINGS_MODULE=openarchiefbeheer.conf.docker_no2fa exec /start.sh" ]
    volumes:
      - ./scripts/docker-compose/imports/openarchiefbeheer-web/docker_no2fa.py:/app/src/openarchiefbeheer/conf/docker_no2fa.py:ro
    healthcheck:
      test: [ "CMD", "python", "-c", "import requests; exit(requests.head('http://localhost:8000/admin/').status_code not in [200, 302])" ]
      interval: 5s
      timeout: 10s
    ports:
      - "8004:8000"
    depends_on:
      openarchiefbeheer-database:
        condition: service_healthy
      redis:
        condition: service_started
    profiles: [ "openarchiefbeheer" ]

  openarchiefbeheer-web-init:
    image: maykinmedia/open-archiefbeheer:2.0.0@sha256:e5217d748869c62d26393311c3cfdcaacf69d8dde80ca3bc172ff41f3885f0ff
    platform: linux/amd64
    environment: *web_env
    command: /setup_configuration.sh
    volumes:
      - ./scripts/docker-compose/imports/openarchiefbeheer-web:/app/setup_configuration
    depends_on:
      openarchiefbeheer-web:
        condition: service_healthy
      redis:
        condition: service_started
    profiles: [ "openarchiefbeheer" ]

  openarchiefbeheer-celery:
    image: maykinmedia/open-archiefbeheer:2.0.0@sha256:e5217d748869c62d26393311c3cfdcaacf69d8dde80ca3bc172ff41f3885f0ff
    platform: linux/amd64
    command: /celery_worker.sh
    environment: *web_env
    healthcheck:
      test: [ "CMD", "python", "/app/bin/check_celery_worker_liveness.py" ]
      interval: 5s
      timeout: 10s
      retries: 10
    depends_on:
      - openarchiefbeheer-database
      - redis
    profiles: [ "openarchiefbeheer" ]

  openarchiefbeheer-celery-beat:
    image: maykinmedia/open-archiefbeheer:2.0.0@sha256:e5217d748869c62d26393311c3cfdcaacf69d8dde80ca3bc172ff41f3885f0ff
    platform: linux/amd64
    command: /celery_beat.sh
    environment: *web_env
    depends_on:
      - openarchiefbeheer-database
      - redis
    profiles: [ "openarchiefbeheer" ]

  openarchiefbeheer-ui:
    image: nginxinc/nginx-unprivileged:1.31.3@sha256:13e6db27178b9bbd0964daa10cfd1027b7d6b17ee718591c9a0e4cb35cbe9d84
    ports:
      - "8005:8080"
    volumes:
      - ./scripts/docker-compose/imports/openarchiefbeheer-ui/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      openarchiefbeheer-web:
        condition: service_healthy
    profiles: [ "openarchiefbeheer" ]

  pabc-database:
    image: postgres:17.10@sha256:5c855ad7b85e68e48a62f34662853f38b57c1c1d80f3a927ab58034fd6d31c5e
    ports:
      - "54329:5432"
    environment:
      - POSTGRES_USER=pabc
      - POSTGRES_PASSWORD=pabc
      - POSTGRES_DB=Pabc
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}" ]
      interval: 5s
      timeout: 10s
      retries: 10
    volumes:
      - ./scripts/docker-compose/volume-data/pabc-database-data:/var/lib/postgresql/data

  pabc-migrations:
    image: ghcr.io/platform-autorisatie-beheer-component/pabc-migrations:1.1.0@sha256:c40905205d997936538bbe1efdf0c582ec98a8414df9ceb05b0c1b0d1121a39a
    platform: linux/amd64
    environment:
      ConnectionStrings__Pabc: "Host=pabc-database;Port=5432;Username=pabc;Password=pabc;Database=Pabc"
      JSON_DATASET_PATH: "/app/json/pabc-mapping-data.json"
    depends_on:
      pabc-database:
        condition: "service_healthy"
    volumes:
      - ./scripts/docker-compose/imports/pabc-database/json-mapping/pabc-mapping-data.json:/app/json/pabc-mapping-data.json:ro

  pabc-api:
    image: ghcr.io/platform-autorisatie-beheer-component/pabc-api:1.1.0@sha256:108225d8ba671ace5be159d24ecfdfe06b073494227943a123955418c5b124a0
    platform: linux/amd64
    environment:
      ASPNETCORE_FORWARDEDHEADERS_ENABLED: "true"
      HTTP_PORTS: "8000"
      API_KEY__0: "zac-test-api-key"
      ConnectionStrings__Pabc: "Host=pabc-database;Port=5432;Username=pabc;Password=pabc;Database=Pabc"
      Oidc__Authority: "http://keycloak:8080/realms/zaakafhandelcomponent/"
      Oidc__ClientId: "pabc"
      Oidc__ClientSecret: "pabcClientSecret"
      Oidc__FunctioneelBeheerderRole: "administrator"
      # in our Docker Compose setup we run Keycloak on HTTP
      Oidc__RequireHttps: "false"
      KeycloakAdmin__ClientId: "pabc-admin-client"
      KeycloakAdmin__ClientSecret: "pabcAdminClientSecret"
    ports:
      - "8006:8000"
    depends_on:
      pabc-database:
        condition: "service_started"
      pabc-migrations:
        condition: "service_completed_successfully"

  zac-database:
    image: postgres:17.10@sha256:5c855ad7b85e68e48a62f34662853f38b57c1c1d80f3a927ab58034fd6d31c5e
    ports:
      - "54320:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}" ]
      interval: 5s
      timeout: 10s
      retries: 20
    environment:
      POSTGRES_USER: zac
      POSTGRES_PASSWORD: password
      POSTGRES_DB: zac
    volumes:
      - ./scripts/docker-compose/imports/zac-database/init-zac-database.sql:/docker-entrypoint-initdb.d/init-zac-database.sql
      - ./scripts/docker-compose/volume-data/zac-database-data:/var/lib/postgresql/data

  zac:
    # By default, we use the most recent ZAC Docker Image. Change this if you wish to use a specific version.
    image: ${ZAC_DOCKER_IMAGE:-ghcr.io/infonl/zaakafhandelcomponent:latest}
    environment:
      - AUTH_ENABLE_PKCE=true
      - AUTH_REALM=zaakafhandelcomponent
      - AUTH_RESOURCE=zaakafhandelcomponent
      - AUTH_SECRET=keycloakZaakafhandelcomponentClientSecret
      - AUTH_SERVER=http://keycloak:8080
      - AUTH_SSL_REQUIRED=${AUTH_SSL_REQUIRED:-none}
      - BAG_API_CLIENT_MP_REST_URL=${BAG_API_CLIENT_MP_REST_URL:-https://api.bag.kadaster.nl/lvbag/individuelebevragingen/v2/}
      - BAG_API_KEY=${BAG_API_KEY:-fakeBagApiKey}
      - BRP_API_CLIENT_MP_REST_URL=http://brp-personen-wiremock:8080/haalcentraal/api/brp
      - BRP_ORIGIN_OIN=${BRP_ORIGIN_OIN:-123443210}
      - BRP_ORIGIN_OIN_HEADER=${BRP_ORIGIN_OIN_HEADER:-x-origin-oin}
      - BRP_PROTOCOLLERING_ENABLED=${BRP_PROTOCOLLERING_ENABLED:-true}
      - BRP_DOELBINDING_PER_ZAAKTYPE=${BRP_DOELBINDING_PER_ZAAKTYPE:-true}
      - BRP_DOELBINDING_HEADER=${BRP_DOELBINDING_HEADER:-x-doelbinding}
      - BRP_DOELBINDING_ZOEKMET=${BRP_DOELBINDING_ZOEKMET:-BRPACT-Totaal}
      - BRP_DOELBINDING_RAADPLEEGMET=${BRP_DOELBINDING_RAADPLEEGMET:-BRPACT-Totaal}
      - BRP_VERWERKING_HEADER=${BRP_VERWERKING_HEADER:-x-verwerking}
      - BRP_VERWERKINGSREGISTER=${BRP_VERWERKINGSREGISTER:-Algemeen}
      - BRP_GEBRUIKER_HEADER=${BRP_GEBRUIKER_HEADER:-x-gebruiker}
      - BRP_TOEPASSING_HEADER=${BRP_TOEPASSING_HEADER:-x-toepassing}
      - BRP_TOEPASSING=${BRP_TOEPASSING:-ZAC}
      - BRP_LOG_LEVEL=INFO
      - BRP_SYSTEM_USER=ZacSystemUser
      - BRP_API_KEY_HEADER=${BRP_API_KEY_HEADER:-}
      - BRP_API_KEY=${BRP_API_KEY:-}
      - BRON_ORGANISATIE_RSIN=${BRON_ORGANISATIE_RSIN:-123443210}
      - CONTEXT_URL=http://localhost:8080
      - DB_HOST=zac-database
      - DB_NAME=zac
      - DB_PASSWORD=password
      - DB_USER=zac
      - GEMEENTE_CODE=${GEMEENTE_CODE:-9999}
      - GEMEENTE_MAIL=${GEMEENTE_MAIL:-gemeente-zac-test@example.com}
      - GEMEENTE_NAAM=${GEMEENTE_NAAM:-FakeZacGemeente}
      - KLANTINTERACTIES_API_CLIENT_MP_REST_URL=http://openklant.local:8000
      - KLANTINTERACTIES_API_TOKEN=fakeToken
      - KEYCLOAK_ADMIN_CLIENT_ID=zaakafhandelcomponent-admin-client
      - KEYCLOAK_ADMIN_CLIENT_SECRET=zaakafhandelcomponentAdminClientSecret
      - KVK_API_CLIENT_MP_REST_URL=${KVK_API_CLIENT_MP_REST_URL:-fakeKvkApiUrl}
      - KVK_API_KEY=${KVK_API_KEY:-fakeKvkApiKey}
      - OFFICE_CONVERTER_CLIENT_MP_REST_URL=http://office-converter:3000
      - OBJECTS_API_CLIENT_MP_REST_URL=http://objecten-api.local:8000
      - OBJECTS_API_TOKEN=fakeZacObjectsToken
      - OPA_API_CLIENT_MP_REST_URL=http://opa:8181
      - OPEN_FORMS_URL=http://localhost:9999 # fake for now
      - OPEN_NOTIFICATIONS_API_SECRET_KEY=openNotificatiesApiSecretKey
      - PABC_API_CLIENT_MP_REST_URL=${PABC_API_CLIENT_MP_REST_URL}
      - PABC_API_KEY=${PABC_API_KEY}
      - SMARTDOCUMENTS_ENABLED=${SMARTDOCUMENTS_ENABLED:-false}
      - SMARTDOCUMENTS_AUTHENTICATION=${SMARTDOCUMENTS_AUTHENTICATION:-fakeSmartDocumentsAuthentication}
      - SMARTDOCUMENTS_CLIENT_MP_REST_URL=${SMARTDOCUMENTS_CLIENT_MP_REST_URL:-http://smartdocuments-wiremock:8080}
      - SMARTDOCUMENTS_FIXED_USER_NAME=${SMARTDOCUMENTS_FIXED_USER_NAME:-}
      - SMTP_SERVER=${SMTP_SERVER:-in-v3.mailjet.com}
      - SMTP_PORT=${SMTP_PORT:-587}
      - SMTP_USERNAME=${SMTP_USERNAME:-fakeMailjetApiKey}
      - SMTP_PASSWORD=${SMTP_PASSWORD:-fakeMailjetApiSecretKey}
      - SIGNALERINGEN_DELETE_OLDER_THAN_DAYS=${SIGNALERINGEN_DELETE_OLDER_THAN_DAYS:-14}
      - SOLR_URL=http://solr:8983
      # Turn off the MicroProfile Open Telemetry subsystem by default because it is not always necessary.
      # Typically, when using the Docker Compose profile 'metrics' you want to set this to 'false'.
      - OTEL_SDK_DISABLED=${OTEL_SDK_DISABLED:-true}
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
      - VERANTWOORDELIJKE_ORGANISATIE_RSIN=${VERANTWOORDELIJKE_ORGANISATIE_RSIN:-316245124}
      - ZAC_INTERNAL_ENDPOINTS_API_KEY=${ZAC_INTERNAL_ENDPOINTS_API_KEY:-fakeZacInternalEndpointsApiKey}
      - ZGW_API_CLIENT_MP_REST_URL=http://openzaak.local:8000/
      - ZGW_API_CLIENTID=zac_client
      - ZGW_API_SECRET=openzaakZaakafhandelcomponentClientSecret
    ports:
      - "8080:8080"
      - "9990:9990"
    # only for integration testing purpose, override to root user
    user: root
    volumes:
      # mount volumes for (optional) JaCoCo test coverage for our integration tests
      # see: https://blog.akquinet.de/2018/09/06/test-coverage-for-containerized-java-apps/
      - ./build/jacoco/itest/jacoco-agent:/jacoco-agent
      - ./build/jacoco/itest/jacoco-report:/jacoco-report
    entrypoint: [
      "sh",
      "-c",
      # entrypoint may be overridden by environment variable to pass on extra arguments
      "${ZAC_DOCKER_ENTRYPOINT:-java -Djboss.bind.address.management=0.0.0.0 -Xms1024m -Xmx1024m -Xlog:gc::time,uptime -jar zaakafhandelcomponent.jar}"
    ]
    deploy:
      resources:
        limits:
          # Set a maximum memory limit for the ZAC container for a more realistic
          # scenario when testing ZAC locally or when running our integration tests.
          memory: 4G
    depends_on:
      zac-database:
        condition: service_healthy
      openzaak.local:
        condition: service_healthy
    profiles: ["zac"]

  objecttypes-api-database:
    image: postgres:17.10@sha256:5c855ad7b85e68e48a62f34662853f38b57c1c1d80f3a927ab58034fd6d31c5e
    ports:
      - "54324:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}"]
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 30s
    environment:
      - POSTGRES_USER=objecttypes
      - POSTGRES_PASSWORD=objecttypes
      - POSTGRES_DB=objecttypes
    profiles: [ "openformulieren" ]

  objecttypes-api:
    image: maykinmedia/objecttypes-api:3.4.2@sha256:d366e6ede1bb924ea351495f4e88ceba53bb0df02fa5302929daef379131fda1
    platform: linux/amd64
    ports:
      - "8011:8000"
    environment: &objecttypes-env
      - CORS_ALLOW_ALL_ORIGINS=true
      - SECRET_KEY=objecttypesApiSecretKey
      - DJANGO_SETTINGS_MODULE=objecttypes.conf.docker
      - DB_HOST=objecttypes-api-database
      - CACHE_DEFAULT=redis:6379/0
      - CACHE_AXES=redis:6379/0
      - DISABLE_2FA=yes
      - DEBUG=true
      - OTEL_SDK_DISABLED=true
      - ALLOWED_HOSTS=*
    depends_on:
      objecttypes-api-database:
        condition: service_healthy
    profiles: [ "openformulieren" ]

  objecttypes-api-import:
    image: maykinmedia/objecttypes-api:3.4.2@sha256:d366e6ede1bb924ea351495f4e88ceba53bb0df02fa5302929daef379131fda1
    platform: linux/amd64
    environment: *objecttypes-env
    command: sh init/init.sh
    volumes:
      - ./scripts/docker-compose/imports/objecttypes-api/fixtures:/app/src/objecttypes/fixtures
      - ./scripts/docker-compose/imports/objecttypes-api/init:/app/init
    depends_on:
      - objecttypes-api
    profiles: [ "openformulieren" ]

  openformulieren-database:
    image: postgres:17.10@sha256:5c855ad7b85e68e48a62f34662853f38b57c1c1d80f3a927ab58034fd6d31c5e
    ports:
      - "54330:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}" ]
      interval: 5s
      timeout: 10s
      retries: 20
    environment:
      - POSTGRES_USER=openformulieren
      - POSTGRES_PASSWORD=openformulieren
      - POSTGRES_DB=openformulieren
    volumes:
      - ./scripts/docker-compose/volume-data/openformulieren-database-data:/var/lib/postgresql/data
    profiles: [ "openformulieren" ]

  openformulieren-init:
    image: openformulieren/open-forms:3.5.5@sha256:086c9f6d2243af224725ba94f66b8a506686d1f09e6a2fa3e8423d47fa61d7b1
    # For now we do not override the platform from AMD to ARM for this Docker Image, because this results in a non-working Open Formulieren instance.
    # Using the INFO.nl Open Formulieren ARM image, when you create a new form and attempt to open it, you will see an error in the browser:
    # "Er ging helaas iets fout. Neem a.u.b. contact op met de helpdesk en informeer hen van dit probleem."
    # This seems to have to do with missing static content (such as JS translation bundles) in the ARM image.
    # You will also see a related 404 error on a JS translation bundle file in the browser when trying to open a form.
    platform: linux/amd64
    environment: &openformulieren-env
      - DJANGO_SETTINGS_MODULE=openforms.conf.docker_local
      - SECRET_KEY=openFormulierenSecretKey
      - DB_NAME=openformulieren
      - DB_USER=openformulieren
      - DB_PASSWORD=openformulieren
      - DB_HOST=openformulieren-database
      - CACHE_DEFAULT=redis:6379/2
      - CACHE_AXES=redis:6379/2
      - CACHE_PORTALOCKER=redis:6379/2
      - CELERY_BROKER_URL=redis://redis:6379/2
      - CELERY_RESULT_BACKEND=redis://redis:6379/2
      - ALLOWED_HOSTS=*
      - CORS_ALLOW_ALL_ORIGINS=true
      - CSRF_TRUSTED_ORIGINS=http://localhost:8007,http://localhost:8009
      - USE_X_FORWARDED_HOST=true
      - EMAIL_HOST=localhost
      - TZ=Europe/Amsterdam
      - DISABLE_2FA=true
      - DJANGO_SUPERUSER_PASSWORD=admin
      - RUN_SETUP_CONFIG=true
      - DEBUG=true
      - OTEL_SDK_DISABLED=true
    command:
      - sh
      - -c
      - |
        set -e
        /setup_configuration.sh
        OTEL_SDK_DISABLED=True src/manage.py shell -c "
        from django.contrib.auth import get_user_model
        User = get_user_model()
        if not User.objects.filter(username='admin').exists():
            User.objects.create_superuser('admin', 'admin@example.com', 'admin')
            print('Admin user created')
        else:
            print('Admin user already exists')
        "
        OTEL_SDK_DISABLED=True src/manage.py shell -c "
        try:
            from openforms.contrib.haal_centraal.models import HaalCentraalConfig
            from zgw_consumers.models import Service
            config = HaalCentraalConfig.get_solo()
            if not config.brp_personen_service_id:
                svc = Service.objects.get(slug='brp-personen')
                config.brp_personen_service = svc
                config.save()
                print('BRP service configured')
            else:
                print('BRP service already configured')
        except Exception as e:
            print(f'BRP config skipped: {e}')
        "
        OTEL_SDK_DISABLED=True src/manage.py shell -c "
        try:
            from openforms.contrib.customer_interactions.models import CustomerInteractionsAPIGroupConfig
            from zgw_consumers.models import Service
            IDENTIFIER = 'klantinteracties-api-group'
            if not CustomerInteractionsAPIGroupConfig.objects.filter(identifier=IDENTIFIER).exists():
                svc = Service.objects.get(slug='openklant-klantinteracties')
                CustomerInteractionsAPIGroupConfig.objects.create(
                    name='Local Klantinteracties API',
                    identifier=IDENTIFIER,
                    customer_interactions_service=svc,
                )
                print('Klantinteracties API group configured')
            else:
                print('Klantinteracties API group already exists')
        except Exception as e:
            print(f'Klantinteracties config skipped: {e}')
        "
        OTEL_SDK_DISABLED=True src/manage.py shell -c "
        from openforms.forms.models import Form
        FORM_SLUG = 'productaanvraag-dimpact-test-formulier-compleet'
        if not Form.objects.filter(slug=FORM_SLUG).exists():
            import os, glob, zipfile
            from openforms.forms.utils import import_form
            json_files = glob.glob('/forms/*.json')
            with zipfile.ZipFile('/tmp/forms.zip', 'w', zipfile.ZIP_DEFLATED) as zf:
                for f in json_files:
                    zf.write(f, os.path.basename(f))
            with open('/tmp/forms.zip', 'rb') as zip_file:
                import_form(zip_file)
            print('Productaanvraag-Dimpact form imported successfully')
        form = Form.objects.get(slug=FORM_SLUG)
        if not form.active:
            form.active = True
            form.save(update_fields=['active'])
            print('Productaanvraag-Dimpact form activated')
        else:
            print('Productaanvraag-Dimpact form already active, skipping import')
        "
    volumes: &openformulieren-volumes
      - openformulieren-static:/srv/static
      - openformulieren-media:/app/media
      - openformulieren-private-media:/app/private_media
      - openformulieren-log:/app/log
      - ./scripts/docker-compose/imports/openformulieren/setup-configuration:/app/setup_configuration
      - ./scripts/docker-compose/imports/openformulieren/settings/docker_local.py:/app/src/openforms/conf/docker_local.py:ro
      - ./scripts/docker-compose/imports/openformulieren/forms:/forms:ro
    depends_on:
      openformulieren-database:
        condition: service_healthy
      redis:
        condition: service_started
      objecttypes-api:
        condition: service_started
    profiles: [ "openformulieren" ]

  openformulieren-web:
    image: openformulieren/open-forms:3.5.5@sha256:086c9f6d2243af224725ba94f66b8a506686d1f09e6a2fa3e8423d47fa61d7b1
    # For now we do not override the platform from AMD to ARM for this Docker Image, because this results in a non-working Open Formulieren instance.
    # Using the INFO.nl Open Formulieren ARM image, when you create a new form and attempt to open it, you will see an error in the browser:
    # "Er ging helaas iets fout. Neem a.u.b. contact op met de helpdesk en informeer hen van dit probleem."
    # This seems to have to do with missing static content (such as JS translation bundles) in the ARM image.
    # You will also see a related 404 error on a JS translation bundle file in the browser when trying to open a form.
    platform: linux/amd64
    environment: *openformulieren-env
    ports:
      - "8009:8000"
    volumes: *openformulieren-volumes
    healthcheck:
      test: [ "CMD", "python", "-c", "import requests; exit(requests.head('http://localhost:8000/admin/').status_code not in [200, 302])" ]
      interval: 10s
      timeout: 10s
      retries: 20
      start_period: 60s
    depends_on:
      openformulieren-database:
        condition: service_healthy
      redis:
        condition: service_started
      openformulieren-init:
        condition: service_completed_successfully
    profiles: [ "openformulieren" ]

  openformulieren-celery:
    image: openformulieren/open-forms:3.5.5@sha256:086c9f6d2243af224725ba94f66b8a506686d1f09e6a2fa3e8423d47fa61d7b1
    # For now we do not override the platform from AMD to ARM for this Docker Image, because this results in a non-working Open Formulieren instance.
    # Using the INFO.nl Open Formulieren ARM image, when you create a new form and attempt to open it, you will see an error in the browser:
    # "Er ging helaas iets fout. Neem a.u.b. contact op met de helpdesk en informeer hen van dit probleem."
    # This seems to have to do with missing static content (such as JS translation bundles) in the ARM image.
    # You will also see a related 404 error on a JS translation bundle file in the browser when trying to open a form.
    platform: linux/amd64
    environment: *openformulieren-env
    command: /celery_worker.sh
    healthcheck:
      test: [ "CMD", "python", "/app/bin/check_celery_worker_liveness.py" ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    volumes:
      - openformulieren-media:/app/media
      - openformulieren-private-media:/app/private_media
      - openformulieren-log:/app/log
      - ./scripts/docker-compose/imports/openformulieren/settings/docker_local.py:/app/src/openforms/conf/docker_local.py:ro
    depends_on:
      openformulieren-database:
        condition: service_healthy
      redis:
        condition: service_started
    profiles: [ "openformulieren" ]

  openformulieren-celery-beat:
    image: openformulieren/open-forms:3.5.5@sha256:086c9f6d2243af224725ba94f66b8a506686d1f09e6a2fa3e8423d47fa61d7b1
    # For now we do not override the platform from AMD to ARM for this Docker Image, because this results in a non-working Open Formulieren instance.
    # Using the INFO.nl Open Formulieren ARM image, when you create a new form and attempt to open it, you will see an error in the browser:
    # "Er ging helaas iets fout. Neem a.u.b. contact op met de helpdesk en informeer hen van dit probleem."
    # This seems to have to do with missing static content (such as JS translation bundles) in the ARM image.
    # You will also see a related 404 error on a JS translation bundle file in the browser when trying to open a form.
    platform: linux/amd64
    environment: *openformulieren-env
    command: /celery_beat.sh
    volumes:
      - openformulieren-media:/app/media
      - openformulieren-private-media:/app/private_media
      - openformulieren-log:/app/log
      - ./scripts/docker-compose/imports/openformulieren/settings/docker_local.py:/app/src/openforms/conf/docker_local.py:ro
    depends_on:
      openformulieren-database:
        condition: service_healthy
      redis:
        condition: service_started
    profiles: [ "openformulieren" ]

  openformulieren-nginx:
    image: nginxinc/nginx-unprivileged:1.31.3@sha256:13e6db27178b9bbd0964daa10cfd1027b7d6b17ee718591c9a0e4cb35cbe9d84
    ports:
      - "8007:8080"
    volumes:
      - ./scripts/docker-compose/imports/openformulieren/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      openformulieren-web:
        condition: service_healthy
    profiles: [ "openformulieren" ]

volumes:
  openformulieren-static:
  openformulieren-media:
  openformulieren-private-media:
  openformulieren-log:
